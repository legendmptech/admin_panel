import React, { createContext } from "react";

const MainContext = createContext();
function MainContextWrapper(props) {
  return <MainContext.Provider>
    
  </MainContext.Provider>
}

export default MainContextWrapper;
